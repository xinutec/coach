//! Per-user pacing settings: the active window and rest spacing that bound when
//! the pacing engine will remind, and the engine's knobs (mode, days per week,
//! emphasis). Home geofence coordinates live on the phone, not here.

pub mod repo;
pub mod types;
