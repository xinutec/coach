// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { DoneSet } from "./DoneSet";
import type { Explanation } from "./Explanation";
import type { Pattern } from "./Pattern";
import type { Substitution } from "./Substitution";
import type { SuggestionKind } from "./SuggestionKind";

export type Suggestion = { exerciseId: number, exerciseName: string, pattern: Pattern, 
/**
 * Work (prescribe) or Assess (measure). Drives the Today card's framing.
 */
kind: SuggestionKind, sets: number, 
/**
 * Sets of this item already logged **today**. The plan is committed at the
 * session's first set; this is the athlete's progress against that
 * commitment, shown on the card. Day-scoped, not session-scoped: the
 * session gap elapses hours before the day does, and the plan forgetting
 * your morning is not something you should have to work around.
 */
done: number, 
/**
 * Those sets' actual numbers, oldest first — `done` in long form. Always
 * `done` entries long.
 */
logged: Array<DoneSet>, repLow: number | null, repHigh: number | null, loadKg: number | null, holdS: number | null, 
/**
 * The muscle group this targets (for the reason text).
 */
group: string, 
/**
 * When set, the ideal exercise for this group genuinely isn't doable here, so
 * an equivalent was swapped in: the ideal's name, and what it would take to do
 * it instead. A swap the athlete can act on ("buy a cable machine", "register
 * your kettlebell weights") rather than an unexplained substitution.
 *
 * Only ever set when the ideal is *actually* blocked. It used to be set
 * whenever the ideal wasn't what the cover picked — which is the normal case,
 * and made the card claim kit was missing that was standing right there.
 */
substitutedFor: Substitution | null, 
/**
 * Why this was chosen (deficit, recovery, ability, readiness). `None` for
 * warm-up items, which are prep rather than a reasoned prescription.
 */
explanation: Explanation | null, };
